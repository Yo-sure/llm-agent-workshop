entrypoint = "langgraph-agent/session_6_mcp_host.py"

modules = ["python-3.11"]

# 호스트만 실행 (호스트가 STDIO로 서버를 자식 프로세스로 실행)
run = "uv run python langgraph-agent/session_6_mcp_host.py"

[nix]
channel = "stable-25_05"

[unitTest]
language = "python3"

[gitHubImport]
requiredFiles = [".replit", "replit.nix"]

[[ports]]
localPort = 8080   # Web UI 포트
externalPort = 80
